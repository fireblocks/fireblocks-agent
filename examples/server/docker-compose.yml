services:
  customer-server:
    image: softhsm2:2.6.1
    depends_on:
      - customer-database
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
      - 35729:35729
  customer-database:
    image: mongo:6
    # volumes:
    #   - database:/data/db
    ports:
      - 27017:27017
# volumes:
# database:
